# Intro Slide

```
   obs: show general slide 
   <CTL-ALT: Start Recording>
    wait 2 seconds
    <CTL-Shift: Pause>
```
```
    obs: move to mugshot
    <CTL-Shift: Resume>
```

## Greetings


Hello, I'm Greg Hughlett of *My Family Social*. In this video, we will walk through the **? Room**. 

```
    <CTL-Shift: Pause>
    obs: show detailed slide 
    <CTL-Shift: Resume>
```

In the ? Room we'll explain:
- the layout of the ? Room
- how to find music posts by searching and filtering
  - When members post music, they are represented in the ? Room on **? cards** that you can easily see and review. 
- In the ? view is where family members can react to the ? post, meaning to like or love it, make a comment or start a discussion thread on a ? post. 
- We'll create a ? post, but first, we'll take a look at the templates used to create a ? post
- Then we'll edit the new post and discuss an important editing rule. 

```
    wait 2 seconds
    <CTL-ALT: Stop Recording>
    Review recording and save as `O1-room-name.mp4` 
```
