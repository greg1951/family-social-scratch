Background
- The poetry terms home page (src/features/poetry/components/poetry-terms-home-page.tsx) is presently laid out in an inefficient way.
- The Term Directory provides a set of cards with a term and a definition below it. Then there is a select button in each.

Request
- Improve the layout of the poetry terms home page to so that instead of cards, there will be a Term Finder and a list of terms listed below it.
  - There will no longer be cards but an entry in the list. 
  - When a term is selected, the on the right side of the page display ther definition of the selected term.
  - There is no reason then to have a Select button for each term. Instead, clicking on the term in the list will select it and display the definition on the right.

- In the heading, there is a section on the right side that contains 3 buttons (View Selected Term, Edit Selected Term, and Add Term).
  - The last two buttons are reserved for admin users.
  - Move all of three buttons to the Term Directory Term Finder section.
  - Add an FAQ link to the right of the Term Finder heading. This link should pass a category of "Poetry Cafe" to the features-faq page.
  - After all buttons are moved safely to the Term Finder section, remove the entire section in the heading that contained them.



