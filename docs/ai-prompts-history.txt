Using the trial-home page as a guide, create a react page in the trial-setup folder for the first step. Create a new shadcn form in a new index.tsx react page that contains a form consist of First Name, Last Name, Nickname, Founder's Email, Password and Confirm Password. Provide Back and Next shadcn buttons for navigation.

Generate a new page in the trial-invite-family folder that consists of a shadcn Dialog that prompts for a family email address and family member name. The result of the dialog submission should be to add it to a list of family members that will be rendered in the page.tsx file in the trial-invite-family folder.

Generate a new page in the trial-invite-family folder that is styled like the page.tsx in the trial-setup-steps folder. This new page should provide a confirmation of the first three steps in that page.tsx and which provides a confirmation in Step 4 of the new family to be created. 

See what you can do to improve the look and feel and navigation in the src/app/(main)/MainTablet.tsx file.

See what you can do to improve the look and feel and navigation in the src/app/(trial)/trial-home/page.tsx file.

See what you can do to improve the look and feel and navigation in the src/app/(trial)/trial-setup-steps/page.tsx file.

suggest a fix for the trial-setup-steps/page.tsx mismatch error on familyNames parameter for CreateTrialAccountSteps component.

When the form in the src/app/(trial)/trial-setup-steps/index.tsx file is submitted, create a dialog to show the status of the 5 update steps performed by the form submission: 1. Add new family name, 2. Create new Member entry, 3. Add Founder credentials, 4. Add invited family members, and 5. Send emails to join new Family Social family. When the Close button on the new dialog is selected, the user will be redirected back to the MainTablet home page.

The status dialog is being triggered when a family member is added in step 2. It should instead be triggered on the step 4 next button.

Remove the status dialog from the step 2 invite family member form submission. 

*** prompted me for more info on the problem I was seeing
When I add a family member in the step 2 dialog, it immediately shows me the update status dialog you just implemented.

*** It took four prompts but the status dialog is working nicely. A bit of cleanup was performed below.

Move the new status dialog component to trial-setup-dialogs/status-update-dialog.tsx and import the new dialog component into the index.tsx where it is run.

*** During testing the family was getting inserted twice. Asked AI about that.

Would the state updates in the form submission for the status dialog cause the SubmitHandler to run more than once?

*** It came back with No, the state update would not do that. It came back with this.
Since a <button> inside a form defaults to submit, that click can trigger both paths.

AI Recommended fix: use only one submit path.

EITHER set “Create Family” to type="button" and keep onClick={submitForm}
OR set it to type="submit" and remove onClick={submitForm}/submitForm() entirely.

*** changed the button to remove onClick and set the type to submit.

Create a new react page.tsx that uses the forms in the src/app/(auth)/(logged-in)/my-account folder with the form contained in src/app/(family)/family-account folder. The new page should use the shadcn Tabs component which is installed in the project. The new page.tsx should be created in src/app/(family)/family-my-account folder.

*** Create phone number formatter

Create a function to be run in a client-side form field onChange event to format a phone number using "(xxx) xxx-xxxx" format, so as the user types in the phone number it will be rendered in the phone form field.



